spring:
  application:
    name: mcp-client
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
  ai:
    openai:
      api-key: sk-12ec0e7dab454d2dbf7829670c001ddc
      base-url: https://api.deepseek.com
      chat:
        options:
          model: deepseek-chat
    mcp:
      client:
        type: SYNC
        request-timeout: 30s
        toolcallback:
          enabled: true
        sse:
          connections:
            mcp-server:
              url: http://localhost:8080
server:
  port: 9090